# MongoDB connection string
   MONGODB_URI=mongodb+srv://<username>:<password>@cluster.mongodb.net/excel_ai_app?retryWrites=true&w=majority

   # Server port
   PORT=5000
   # JWT secret
   JWT_SECRET=your_jwt_secret_here
